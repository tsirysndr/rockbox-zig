[package]
name = "rockbox-wasm"
version = "0.1.0"
edition = "2021"
description = "Rockbox daemon compiled to WebAssembly — exposes rb_* C-ABI exports for JS control"

[lib]
crate-type = ["staticlib"]

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
